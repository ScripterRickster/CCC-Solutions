#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
	int s,m,l;
    cin >> s >> m >> l;

    (s + (2*m) + (3*l)) > 9 ? cout << "happy" : cout << "sad";
    
}
